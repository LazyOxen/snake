# A Snake clone

Super basic snake clone written in C++. Uses the win32 api, so windows only.

If laziness doesn't overcome me, will modify further so it stops doing some annoying things - e.g. exiting immediately after you die.

Only tested on windows 7 with 64bit tdm-gcc 5.1.0
